Flask>=2.2
gunicorn>=21.2
flask-cors>=3.0
# Pinned near compromised version for SCA demo (TeamPCP CanisterWorm supply chain attack).
# 1.82.7 was removed from PyPI; 1.82.6 is the closest installable version.
# Datadog SCA flags it via advisory version-range matching.
litellm==1.82.6
ddtrace
python-json-logger
